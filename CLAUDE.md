# MCP Azure DevOps Tools

Spring Boot starter MCP per Azure DevOps (work items, repos, pipelines, boards, releases). Maven Central: `io.github.massimilianopili:mcp-devops-tools`.

## Build

```bash
/opt/maven/bin/mvn clean compile
/opt/maven/bin/mvn clean install -Dgpg.skip=true
/opt/maven/bin/mvn clean deploy
```

Java 17+. Maven: `/opt/maven/bin/mvn`.

Tool, struttura, configurazione e dipendenze: vedi [README.md](README.md).
Ricerca semantica: `embeddings_search_docs("mcp-devops-tools")`
